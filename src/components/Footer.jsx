import React from "react";
import "./Footer.css";
export default function Footer() {
  return (
    <div className="footer">
      <p>
        This project in work in progress.There is many links that don't work
        due to api's not working together and not filtering anything.Only
        movies and series that is hosted on vidsrc servers are working.
      </p>
    </div>
  );
}
